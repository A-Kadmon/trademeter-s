import App from './App.svelte';

var app = new App({
	target: document.body
});

export default app;

// import {CategoryScale, Chart as ChartJS, LinearScale, LineController, LineElement, PointElement, Tooltip} from 'chart.js';
//
// export {CategoryScale, ChartJS as Chart , LinearScale, LineController, LineElement, PointElement, Tooltip}
